@model EduBalance.Models.Schedule

@{
    ViewData["Title"] = "Add Class";
}

<style>
    .schedule-page {
        max-width: 780px;
        margin: 0 auto;
    }

    .schedule-card {
        background: rgba(255,255,255,0.88);
        border-radius: 28px;
        padding: 30px;
        box-shadow: 0 12px 30px rgba(0,0,0,0.08);
        border: 1px solid rgba(0,0,0,0.06);
    }

    html[data-theme="dark"] .schedule-card {
        background: rgba(23, 28, 39, 0.92);
        border: 1px solid rgba(255,255,255,0.08);
    }

    .page-menu {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-bottom: 20px;
    }

        .page-menu a {
            text-decoration: none;
            padding: 10px 14px;
            border-radius: 14px;
            background: rgba(255,255,255,0.9);
            color: #222;
            font-weight: 600;
            transition: 0.25s ease;
            border: 1px solid rgba(0,0,0,0.06);
        }

            .page-menu a:hover {
                background: rgba(255, 62, 165, 0.12);
                color: #e52f91;
                transform: translateY(-2px);
            }

    html[data-theme="dark"] .page-menu a {
        background: rgba(23,28,39,0.9);
        color: #eef2ff;
        border: 1px solid rgba(255,255,255,0.08);
    }

    .schedule-title {
        font-size: 2rem;
        font-weight: 800;
        margin-bottom: 6px;
    }

    .schedule-subtitle {
        color: #6c757d;
        margin-bottom: 24px;
    }

    html[data-theme="dark"] .schedule-subtitle {
        color: #b6bfd3;
    }

    .field-block {
        margin-bottom: 18px;
    }

        .field-block label {
            font-weight: 700;
            margin-bottom: 8px;
            display: block;
        }

    .field-input {
        width: 100%;
        border-radius: 18px;
        padding: 14px 16px;
        border: 1px solid rgba(0,0,0,0.08);
        transition: 0.25s ease;
        background: #fff;
    }

        .field-input:focus {
            outline: none;
            border-color: #ff3ea5;
            box-shadow: 0 0 0 4px rgba(255, 62, 165, 0.12);
        }

    html[data-theme="dark"] .field-input {
        background: #1f2633;
        color: #eef2ff;
        border: 1px solid rgba(255,255,255,0.08);
    }

    .action-row {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        margin-top: 22px;
    }

    .btn-main, .btn-soft {
        border: none;
        text-decoration: none;
        border-radius: 16px;
        padding: 12px 18px;
        font-weight: 700;
        transition: all 0.25s ease;
    }

    .btn-main {
        background: linear-gradient(135deg, #ff3ea5, #e52f91);
        color: white;
        box-shadow: 0 10px 24px rgba(255, 62, 165, 0.22);
    }

        .btn-main:hover {
            transform: translateY(-2px);
            color: white;
        }

    .btn-soft {
        background: #eef1f7;
        color: #222;
    }

        .btn-soft:hover {
            background: rgba(255, 62, 165, 0.12);
            color: #e52f91;
        }

    html[data-theme="dark"] .btn-soft {
        background: #1f2633;
        color: #eef2ff;
    }
</style>

<div class="schedule-page">
    <div class="page-menu">
        <a asp-controller="Home" asp-action="Index">Dashboard</a>
        <a asp-controller="MoodLogs" asp-action="Create">Mood</a>
        <a asp-controller="Stress" asp-action="Create">Stress</a>
        <a asp-controller="StudyBalance" asp-action="Create">Study Balance</a>
    </div>

    <div class="schedule-card">
        <h2 class="schedule-title">Add Class</h2>
        <p class="schedule-subtitle">Keep your classes and timetable organized.</p>

        <form asp-action="Create" method="post">
            @Html.AntiForgeryToken()

            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <div class="field-block">
                <label asp-for="Day"></label>
                <select asp-for="Day" class="field-input">
                    <option value="">Select day</option>
                    <option>Monday</option>
                    <option>Tuesday</option>
                    <option>Wednesday</option>
                    <option>Thursday</option>
                    <option>Friday</option>
                </select>
            </div>

            <div class="field-block">
                <label asp-for="Subject"></label>
                <input asp-for="Subject" class="field-input" placeholder="Information Systems" />
            </div>

            <div class="field-block">
                <label asp-for="Time"></label>
                <input asp-for="Time" type="time" class="field-input" />
            </div>

            <div class="field-block">
                <label asp-for="Location"></label>
                <input asp-for="Location" class="field-input" placeholder="LR20" />
            </div>

            <div class="field-block">
                <label asp-for="Type"></label>
                <select asp-for="Type" class="field-input">
                    <option value="">Select type</option>
                    <option>Lecture</option>
                    <option>Tutorial</option>
                    <option>Lab</option>
                </select>
            </div>

            <div class="action-row">
                <a asp-action="Index" class="btn-soft">View Schedule</a>
                <button type="submit" class="btn-main">Add Class</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}